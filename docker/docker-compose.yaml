version: '3'

services:
  app:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    ports:
      - "5000:5000"

  postgres:
    image: postgres:15
    environment:
      POSTGRES_PASSWORD: admin